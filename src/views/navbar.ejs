<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime World</title>
    <style>
        /* Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #0f0f1a;
        }

        /* Navigation Bar */
        .anime-nav {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            box-shadow: 0 4px 30px rgba(255, 0, 255, 0.2);
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Logo */
        .logo {
            display: flex;
            align-items: center;
            color: #fff;
            text-decoration: none;
            font-size: 1.8rem;
            font-weight: 700;
            text-shadow: 0 0 10px rgba(255, 0, 255, 0.7);
            transition: all 0.3s ease;
        }

        .logo:hover {
            text-shadow: 0 0 15px rgba(255, 0, 255, 1);
            transform: scale(1.05);
        }

        .logo span {
            color: #ff00ff;
        }

        /* Navigation Links */
        .nav-links {
            display: flex;
            list-style: none;
        }

        .nav-links li {
            margin-left: 30px;
            position: relative;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 1.1rem;
            font-weight: 500;
            padding: 8px 12px;
            border-radius: 5px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #ff00ff, #00ffff);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::before {
            width: 100%;
        }

        .nav-links a:hover {
            color: #00ffff;
            text-shadow: 0 0 5px rgba(0, 255, 255, 0.5);
        }

        /* Dropdown Menu */
        .dropdown {
            position: relative;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background-color: #1a1a2e;
            min-width: 200px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            border-radius: 0 0 5px 5px;
            z-index: 1;
            border-top: 2px solid #ff00ff;
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.3s ease;
        }

        .dropdown:hover .dropdown-content {
            display: block;
            opacity: 1;
            transform: translateY(0);
        }

        .dropdown-content a {
            color: #fff;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            transition: all 0.2s ease;
        }

        .dropdown-content a:hover {
            background-color: #16213e;
            color: #00ffff;
            padding-left: 20px;
        }

        /* Search Bar */
        .search-box {
            display: flex;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            padding: 8px 15px;
            transition: all 0.3s ease;
        }

        .search-box:hover {
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 0 10px rgba(255, 0, 255, 0.3);
        }

        .search-box input {
            background: transparent;
            border: none;
            outline: none;
            color: #fff;
            padding: 5px 10px;
            width: 200px;
        }

        .search-box input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        .search-box button {
            background: transparent;
            border: none;
            color: #fff;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .search-box button:hover {
            color: #00ffff;
            transform: scale(1.1);
        }

        /* User Profile */
        .user-profile {
            display: flex;
            align-items: center;
            cursor: pointer;
        }

        .user-profile img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #ff00ff;
            transition: all 0.3s ease;
        }

        .user-profile:hover img {
            border-color: #00ffff;
            transform: scale(1.1);
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
        }
    </style>
</head>

<body>
    <nav class="anime-nav">
        <a href="#" class="logo">
            <%= username %><span>World</span>
        </a>

        <ul class="nav-links">
            <li><a href="#">Home</a></li>
            <li class="dropdown">
                <a href="#">Genres</a>
                <div class="dropdown-content">
                    
                </div>
            </li>
            <li><a href="#">Popular</a></li>
            <li><a href="#">Seasonal</a></li>
            <li><a href="#">Movies</a></li>
            <li><a href="#">News</a></li>
        </ul>

        <div class="search-box">
            <input type="text" id="usernameInput" placeholder="Enter username">
            <button id="searchButton">🔍</button>
        </div>

        <div class="user-profile">
            <img src="https://i.pravatar.cc/150?img=5" alt="User Profile">
        </div>
    </nav>
    <script>
        document.getElementById("searchButton").addEventListener("click", function() {
            const username = document.getElementById("usernameInput").value.trim(); // Get the username input
            if (username) {
                // Redirect to the user channel profile page
                window.location.href = `http://localhost:8000/api/v1/users/index/${username}`; // Adjust the URL as necessary
            } else {
                alert("Please enter a username."); // Optional: Alert if the input is empty
            }
        });
    </script>
</body>

</html>